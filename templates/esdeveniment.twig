{% extends "base.twig" %}

{% block content %}
    <!-- .main .wrap-blanc -->
    <main class="main wrap-blanc blog">
    <!-- .main-header -->
    <header class="main-header">
        <div class="container-fluid">
            <div class="row">
                <div class="col-xxs-12 col-xs-8 col-sm-9">
                    <h1>{{post.title}}</h1>
                    {% include "share_links.twig" %}
                </div>
                {% if attribute(post,'wpcf-call_to_action_top_url') %}
                    <div class="col-xxs-12 col-xs-4 col-sm-3">
                        <a href="{{ attribute(post,'wpcf-call_to_action_top_url') }}" class="bt-basic bt-basic-vermell">{{ attribute(post,'wpcf-call_to_action_top_text') }}</a>
                    </div>
                {% endif %}
            </div>
        </div>
    </header><!--/.main-header -->

    <div class="container-fluid">
        <div class="row">

            <!-- .contingut -->
            <div class="contingut contingut-left col-sm-9">
                <!-- .contingut-article -->
                <article class="contingut-article">

                    <!-- .contingut-header -->
                    <header class="contingut-header">
                        <!-- .info-esdeveniment -->
                        <div class="info-esdeveniment">
                            <div class="row">
                                <div class="col-sm-3">
                                    Cicle o sèrie:
                                </div>
                                <div class="col-sm-9">
                                    {% for term in post.terms('categories') %}
                                        <a href="{{term.link}}">{{term.name}}</a>{% if not loop.last %}, {% endif %}
                                    {% endfor %}
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-sm-3">
                                    Data:
                                </div>
                                <div class="col-sm-9">
                                    {{ attribute(post,'wpcf-data_inici')|date('j M Y') }}{% if attribute(post,'wpcf-data_fi') %} - {{ attribute(post,'wpcf-data_fi')|date('j M Y') }}{% endif %}
                                </div>
                            </div>
                            {% if attribute(post,'wpcf-horari') %}
                            <div class="row">
                                <div class="col-sm-3">
                                    Hora:
                                </div>
                                <div class="col-sm-9">
                                    {{ attribute(post,'wpcf-horari') }}
                                </div>
                            </div>
                            {% endif %}
                            <div class="row">
                                <div class="col-sm-3">
                                    Lloc:
                                </div>
                                <div class="col-sm-9">
                                    {{ attribute(post,'wpcf-lloc') }}
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-sm-3">
                                    Cost:
                                </div>
                                <div class="col-sm-9">
                                {% if not attribute(post,'wpcf-cost') %}
                                    Gratuït
                                {% else %}
                                    {{ attribute(post,'wpcf-cost') }}€
                                {% endif %}
                                </div>
                            </div>
                        </div><!--/.info-esdeveniment -->

                        {% if post.thumbnail.src %}
                            <img alt="{{post.title}}" src="{{post.thumbnail.src}}">
                            {% if post.thumbnail.caption %}
                                <em class="peu-foto">{{post.thumbnail.caption}}</em>
                            {% endif %}
                        {% endif %}
                        {% if post.terms('tags') %}
                            <div class="bt-grup">
                            {% for term in post.terms('tags') %}
                                <a class="bt-basic" href="{{term.link}}">{{term.name}}</a>
                            {% endfor %}
                            </div>
                        {% endif %}
                        <div class="data">
                            <time itemprop="dateCreated" datetime="{{post.post_date|date('Y-n-j')}}">{{post.post_date|date('j M Y')}}</time><span>|</span>Publicat per <a href="{{post.author.path}}">{{ post.author.name }}</a>
                        </div>
                        <hr class="clara">
                    </header><!--/.contingut-header -->

                    <!-- .contingut-section -->
                    <section class="contingut-section article-cont">
                        {{post.content}}

                        <div class="mapa">
                            <iframe width="100%" height="375" frameborder="0" scrolling="no" marginheight="0" marginwidth="0" src="http://www.openstreetmap.org/export/embed.html?bbox=2.1620213985443115%2C41.38357884350577%2C2.1761834621429443%2C41.39064622932158&amp;layer=mapnik" style="border: 1px solid black"></iframe><br/><small><a href="http://www.openstreetmap.org/#map=17/41.38711/2.16910">Ver mapa más grande</a></small>
                        </div>

                    </section><!--/.contingut-section -->

                </article><!--/.contingut-article -->
            </div><!--/.contingut -->

            {% include "sidebar-news-single.twig" %}
        </div><!--/.row -->
    </div><!--/.container-fluid -->
    </main><!--/.main .wrap-blanc -->


{% endblock %}