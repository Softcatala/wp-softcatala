    No s'han trobat coincidències per a la cerca «{{ paraula }}» en el diccionari de sinònims.
    <ul>
        {% if suggestions %}
            {% include 'ajax/sinonims-alternatives.twig' with { 'alternatives': suggestions } only %}
        {%  endif %}
    </ul>
