{% if recursos %}

  <h3>Recursos i serveis</h3>
  <h1>Viure i treballar en català</h1>

  <div class="row">
    <!-- .home-thumbs  -->
    <div class="home-thumbs col-md-12 col-lg-8">
      <div class="row">
        {% for recurs in recursos %}
        <div class="col-xxs-12 col-xs-6 col-sm-6">
          <a href="{{ recurs.link }}" title="{{ recurs.title }}" style="{{ recurs.image | home_thumb }}">
            <h2>{{ recurs.title }}</h2>
            <p>{{{ recurs.description }}}</p>
          </a>
        </div>
      </div>
    </div><!--/.home-thumbs  -->
  </div><!--/.row  -->

{% endif %}